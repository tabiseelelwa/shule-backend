const express = require("express");
const router = express.Router();
const Bdd = require("../bdd/connexion");


router.get('/nouvpaiement', (req, res)=>{
    const numpaie = PAIE + "_" + Date.now()
    
})